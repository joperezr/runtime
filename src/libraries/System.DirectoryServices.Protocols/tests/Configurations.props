<Project>
  <PropertyGroup>
    <BuildConfigurations>
        $(NetCoreAppCurrent)-Windows_NT;
        $(NetCoreAppCurrent)-Linux;
        $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
